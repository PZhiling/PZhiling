--- src/components/ResultTabs.tsx
+++ src/components/ResultTabs.tsx
@@ -3227,7 +3227,7 @@
         {activeTab === "p3" && phase3 && (
           <ScriptHighlighter
-            content={phase3.replace(/^##?\s*Phase 3.*/i, "").trim()}
+            content={phase3.replace(/(?:^|\n)(?:#+\s*|\*\*\s*)?Phase 3[^\n]*/i, "").trim()}
             fullResult={result}
             onUpdateContent={handleUpdatePhase3}
--- src/App.tsx
+++ src/App.tsx
@@ -658,5 +658,5 @@
               // Replace Phase 3
               updatedResult = content.replace(
-                /(##?\s*Phase 3[\s\S]*)$/i,
+                /((?:^|\n)(?:#+\s*|\*\*\s*)?Phase 3[\s\S]*)$/i,
                 "## Phase 3: Voiceover Script\n\n" + updated_phase_3,
               );
@@ -664,5 +664,5 @@
               // Append to Phase 2
               const phase2Match = updatedResult.match(
-                /(##?\s*Phase 2[^\n]*\n(?:.*?))(##?\s*Phase 3)/is,
+                /((?:^|\n)(?:#+\s*|\*\*\s*)?Phase 2[^\n]*\n(?:.*?))((?:^|\n)(?:#+\s*|\*\*\s*)?Phase 3)/is,
               );
               if (phase2Match) {
@@ -686,5 +686,5 @@
 
           const phase2Match = content.match(
-            /(##?\s*Phase 2[^\n]*\n(?:.*?))(##?\s*Phase 3)/is,
+            /((?:^|\n)(?:#+\s*|\*\*\s*)?Phase 2[^\n]*\n(?:.*?))((?:^|\n)(?:#+\s*|\*\*\s*)?Phase 3)/is,
           );
           if (phase2Match) {
@@ -755,5 +755,5 @@
     // Extract only Phase 3 (Content Creation) if possible
-    const match = result.match(/##\s*Phase 3[\s\S]*/i);
+    const match = result.match(/(?:^|\n)(?:#+\s*|\*\*\s*)?Phase 3[\s\S]*/i);
     const content = match ? match[0] : result;
 
@@ -773,5 +773,5 @@
     // 1. Extract only Phase 3 (Content Creation) which has the Visual & B-Roll list or table
-    const phase3Match = result.match(/##\s*Phase 3[\s\S]*/i);
+    const phase3Match = result.match(/(?:^|\n)(?:#+\s*|\*\*\s*)?Phase 3[\s\S]*/i);
     const phase3Content = phase3Match ? phase3Match[0] : result;
